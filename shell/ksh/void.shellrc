pkg() {
    if [ -z "$1" ]; then
        return
    fi

    flag=${1#-}
    shift
    case $flag in
        a|u) sudo xbps-install -Su "$@" ;;
        d) sudo xbps-remove -R "$@" ;;
        i) xbps-query --repository --show "$@" ;;
        s) xbps-query --repository -s "$@" ;;
    esac
}
